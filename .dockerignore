# ===========================================
# Dependencies (not needed in image)
# ===========================================
node_modules

# ===========================================
# Build outputs (we build fresh in Docker)
# ===========================================
dist

# ===========================================
# Environment files (secrets via Docker secrets)
# ===========================================
.env
.env.*
!.env.example

# ===========================================
# Logs
# ===========================================
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# ===========================================
# Git & Version Control
# ===========================================
.git
.gitignore
.gitattributes

# ===========================================
# Editor
# ===========================================
.vscode

# ===========================================
# Docker files (not needed inside the image)
# ===========================================
Dockerfile*
compose*.yaml
Caddyfile
.dockerignore

# ===========================================
# Tests & Documentation (not needed at runtime)
# ===========================================
test
docs
*.md
!README.md

# ===========================================
# OS files
# ===========================================
.DS_Store
Thumbs.db

# ===========================================
# Development tools (not needed in production)
# ===========================================
.husky
commitlint.config.js
eslint.config.mjs
nodemon.json
tsconfig.test.json
vitest.config.ts
.npmrc

# ===========================================
# Prisma config (schema is needed, config is not)
# ===========================================
prisma.config.ts
